@model IEnumerable<Model.SolicitacaoDeCadastro>
@{
    Layout = "_LayoutCliente";
    ViewData["Title"] = "DetailsMultiple";
}

<h1>Solicitações De Cadastro</h1>
<br />
<div>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodSolicitacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumRegistro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumCnh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataNascimento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>

            </th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td class="col-sm-10">
                    @Html.DisplayFor(modelItem => item.CodSolicitacao)
                </td>

                <td class="col-sm-10">
                    @Html.DisplayFor(modelItem => item.NumRegistro)
                </td>

                <td class="col-sm-10">
                    @Html.DisplayFor(modelItem => item.NumCnh)
                </td>

                <td class="col-sm-10">
                    @Html.DisplayFor(modelItem => item.DataNascimento)
                </td>

                <td class="col-sm-10">
                    @Html.DisplayFor(modelItem => item.Status)
                </td>

                <td>
                    @if (item.Status == "reprovada" && ViewBag.solicitacaoEmAndamento)
                    {

                        @Html.ActionLink("Reenviar Solicitacao", "Create", new { id = item.CodUsuarioEntregador })

                    }
                </td>
            </tr>
        }
    </table>

</div>

